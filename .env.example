# Configurações do Servidor
PORT=8080
SPRING_PROFILES_ACTIVE=dev

# Configurações do Banco de Dados MongoDB
# Para desenvolvimento local (opcional - usa MongoDB local por padrão)
MONGODB_URI=mongodb://localhost:27017/wellora

# Para produção no MongoDB Atlas (substitua com suas credenciais)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&appName=Cluster0

# Configurações JWT
# IMPORTANTE: Use chaves seguras em produção de pelo menos 32 caracteres!
# Gere chaves em: https://generate-secret.vercel.app/32
JWT_SECRET=sua-chave-secreta-jwt-super-segura-aqui-min-32-chars
JWT_EXPIRATION=86400
JWT_ANONYMOUS_SECRET=sua-chave-anonima-jwt-super-segura-aqui-min-32-chars
JWT_ANONYMOUS_EXPIRATION=3600

# Configurações de Log
LOG_LEVEL=DEBUG